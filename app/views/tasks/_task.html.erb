<% edit_path = edit_task_path(task, return_to: request.path) %>
<% tint = project_tint(task.project) unless task.overdue? %>
<%= tag.tr class: ("has-background-danger-light" if task.overdue?),
           style: ("background-color: #{tint}" if tint) do %>
  <td class="is-narrow">
    <%= button_to complete_task_path(task), method: :patch, class: "button is-small is-white", form_class: "is-inline-block", "aria-label": "Complete #{task.title}" do %>
      <span class="icon">
        <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="1.8">
          <rect x="4" y="4" width="16" height="16" rx="4"/>
        </svg>
      </span>
    <% end %>
  </td>
  <td>
    <%= priority_badge(task) %>
    <strong><%= link_to task.title, edit_path, class: "task-title" %></strong>
    <% if task.due_at %>
      <span class="tag is-info is-light"><%= due_tag(task) %></span>
    <% end %>
    <% if task.recurrence.present? %>
      <span class="tag is-light">↻ <%= task.recurrence %></span>
    <% end %>
    <% task.labels.sort_by(&:name).each do |label| %>
      <span class="tag is-light"><%= label.name %></span>
    <% end %>
  </td>
  <td class="is-narrow">
    <% if task.recurrence.present? %>
      <%= button_to skip_task_path(task), method: :patch, class: "button is-small is-white", form_class: "is-inline-block", "aria-label": "Skip #{task.title} to next occurrence" do %>
        <span class="icon">
          <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
            <path d="M5 12h14M13 6l6 6-6 6"/>
          </svg>
        </span>
      <% end %>
    <% end %>
    <%= link_to edit_path, class: "button is-small is-white", "aria-label": "Edit #{task.title}" do %>
      <span class="icon">
        <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
          <path d="M16.86 4.49l2.65 2.65M3 21l4.5-1.13a4.5 4.5 0 001.9-1.13l9.36-9.36a1.87 1.87 0 000-2.65l-.94-.94a1.87 1.87 0 00-2.65 0L5.26 15.6a4.5 4.5 0 00-1.13 1.9L3 21z"/>
        </svg>
      </span>
    <% end %>
    <%= button_to task_path(task), method: :delete, class: "button is-small is-white has-text-danger", form_class: "is-inline-block", "aria-label": "Delete #{task.title}", data: { turbo_confirm: "Delete this task?" } do %>
      <span class="icon">
        <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
          <path d="M4 7h16M10 11v6M14 11v6M5 7l1 13a2 2 0 002 2h8a2 2 0 002-2l1-13M9 7V4a1 1 0 011-1h4a1 1 0 011 1v3"/>
        </svg>
      </span>
    <% end %>
  </td>
<% end %>
